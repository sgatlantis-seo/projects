[project]
name = "projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sgatlantis-seo", email = "sgatlantis.seo@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "keras",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "plotly>=7.1.0",
    "seaborn>=0.13.2",
    "streamlit>=1.64.0",
    "tensorflow",
]

[project.scripts]
projects = "projects:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
